// Implements a simple http://last.fm API client library.
//
// The library currently doesn't support authentication, so it only implements
// a few queries that are useful without authentication.
package lastfm
